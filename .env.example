# Zenvi Core — Frontend Environment Configuration
# Copy this file to .env and adjust the backend URL if needed.

# Backend API server URL (zenvi-backend must be running)
ZENVI_BACKEND_URL=http://localhost:8500
